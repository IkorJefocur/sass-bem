# SASS-BEM - Миксин `preset-def`

`preset-def(Mod $names...)`

Объявляет пресет(ы) `$names` для текущего блока или элемента. Если в текущем контексте присутствует модификатор, он будет скомбинирован с пресетом.