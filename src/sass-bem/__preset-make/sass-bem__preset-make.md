# SASS-BEM - Функция `preset-make`

`string preset-make(Mod $name, Entity $entity = null)`

Создает селектор для пресета с именем `$name` для контекста `$entity`. Если `$entity` не задан, будет использоваться текущий контекст.